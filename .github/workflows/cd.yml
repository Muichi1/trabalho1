name: Continuous Deployment

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout do código
        uses: actions/checkout@v3

      - name: Fazer Deploy (Exemplo com Heroku)
        run: |
          echo "Iniciando Deploy..."
          # Simulação de deploy, aqui você pode adicionar comandos específicos 
